% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git_push.R
\name{git_push}
\alias{git_push}
\title{Send Git Commands to RStudio Terminal}
\usage{
git_push(
  message = paste(format(Sys.time(), "\%Y\%m\%d \%H:\%M"), "Backup"),
  add = TRUE,
  push = FALSE,
  remote = "origin main"
)
}
\arguments{
\item{message}{A string. The commit message. Defaults to a timestamp backup
message (e.g., "20260217 14:35 Backup).}

\item{add}{Logical. If TRUE (default), stages all changes with
"git add ." before committing.}

\item{push}{Logical. If TRUE, pushes changes to the remote repository
after committing. Defaults to FALSE.}

\item{remote}{A string. The remote and branch to push to, in the format
"remote branch" (e.g., "origin main"). Defaults to "origin main".
Only used when push is TRUE.}
}
\value{
Invisibly returns NULL.
}
\description{
Stages, commits, and optionally pushes changes to a git repository
directly from R using the RStudio terminal.
}
\examples{
\dontrun{
# This function only works in an interactive RStudio session and will
# never be run during R CMD check.
git_commit(message = "Updated analysis script")
git_commit(message = "Updated analysis script", push = TRUE)
git_commit(message = "Updated analysis script", push = TRUE,
           remote = "origin dev")
}
}
