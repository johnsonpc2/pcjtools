% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_subtitle.R
\name{make_contrast_subtitle}
\alias{make_contrast_subtitle}
\title{Build a ggtext-compatible subtitle from emmeans contrast results}
\usage{
make_contrast_subtitle(
  contrasts,
  labels,
  header = NULL,
  separator = "<br>",
  footnote = NULL,
  signed = TRUE
)
}
\arguments{
\item{contrasts}{A summary()ed emmeans contrast object (a data frame with
columns \code{t.ratio}, \code{df}, and \code{p.value}).}

\item{labels}{Character vector of condition labels, one per row of
\code{contrasts}, in the same order as the rows.}

\item{header}{Optional character string printed as a bold header above the
contrast rows (e.g. \code{"Recogniser vs. Non-recogniser"}). \code{NULL} omits it.}

\item{separator}{String placed between contrast rows. Use \code{"<br>"} (default)
for one contrast per line, or \code{"; "} to run them inline.}

\item{footnote}{Optional character string appended after the last contrast
row, preceded by a \code{"<br>"}. Supports ggtext markdown. \code{NULL} omits it.}

\item{signed}{Logical. Should Cohen's d preserve the sign of the t-ratio?
Defaults to \code{TRUE}. Set to \code{FALSE} to always report \|d\|.}
}
\value{
A single character string of ggtext-compatible markdown, suitable
for use in ggplot2::labs(subtitle = ...).
}
\description{
Generates a markdown string summarising one or more contrast tests,
formatted for use with ggtext::element_markdown() in a ggplot2
subtitle. Designed to work with any emmeans::contrast() summary
object, so it can be reused across plot types.
}
\details{
Each contrast row is reported as:
\verb{label: *t*(df) = t.ratio, *p* p.value, *d* = cohen.d}

Cohen's d is computed via effectsize::t_to_d() with \code{signed = TRUE},
which preserves the direction of the effect (negative when the contrast
t-ratio is negative).
}
\examples{
\dontrun{
# Assuming `my_contrasts` is a summary()ed emmeans contrast object:
subtitle <- make_contrast_subtitle(
  contrasts = my_contrasts,
  labels    = c("Intact vs. Non-intact"),
  header    = "Primary contrast",
  footnote  = "Bonferroni corrected"
)

ggplot2::ggplot(my_data, ggplot2::aes(x, y)) +
  ggplot2::geom_point() +
  ggplot2::labs(subtitle = subtitle) +
  theme_pcj() +
  ggplot2::theme(
    plot.subtitle = ggtext::element_markdown(lineheight = 1.2)
  )
}
}
